export interface IUser {
  name: string;
  email: string;
  country_code: string;
  cellphone: string;
  project_type: string;
  budget: string;
  message?: string;
}

export interface IWhats {
  name: string;
  email: string;
  cellphone: string;
}
